#.travis.yml

language: node_js
node_js:
  - "6.9"

branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools
  - npm install -g @angular/cli
  - cd front-end
  - npm install
  # TODO: Uncomment these when the back end's up and running
  # - cd ../back-end
  # - npm install

script:
  - echo "$TRAVIS_BRANCH"
  - ng build --prod
  - cd ..
  - if [[$TRAVIS_BRANCH=master]]; then firebase deploy; fi;

after_success:
  - echo 'BUILD SUCCESS'
